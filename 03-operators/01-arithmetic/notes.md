- +, -, *, /, **, % this are the comman operator in js and python

- python
    - we don't have ++ (Incremental) and -- (Incremental) operators
    - we have // (Floor Division), which will discard the floating values